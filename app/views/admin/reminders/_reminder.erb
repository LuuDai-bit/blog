<tr>
  <td><%= reminder.title.truncate 50 %></td>
  <td><%= reminder.content.truncate 50 %></td>
  <td><%= day_format(reminder.day) %></td>
  <td><%= time_format((reminder.hour - reminder.original_timezone)%24, reminder.minute) %></td>
  <td><%= reminder.only_once ? 'yes' : '-' %></td>
  <td>
    <%= link_to 'Edit', edit_admin_reminder_path(reminder), class: 'btn btn-light' %>
    <%= link_to 'Destroy', admin_reminder_path(reminder), method: :delete, class: 'btn btn-danger' %>
  </td>
</tr>
